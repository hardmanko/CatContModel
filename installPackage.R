
#Install dependencies. 
install.packages(c("Rcpp", "polspline", "CircStats", "msm"))

#Put in the path to the package file and the version.
install.packages("XXX_where_you_put_the_zip_contents_XXX/packaged/CatContModel_V.V.V.zip", repos=NULL)
